<%= form_for(@ad_rate) do |f| %>
  <% if @ad_rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ad_rate.errors.count, "error") %> prohibited this ad_rate from being saved:</h2>

      <ul>
      <% @ad_rate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ad_space_id %><br />
    <%= f.number_field :ad_space_id %>
  </div>
  <div class="field">
    <%= f.label :rate %><br />
    <%= f.text_field :rate %>
  </div>
  <div class="field">
    <%= f.label :frequency_id %><br />
    <%= f.number_field :frequency_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
